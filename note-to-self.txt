MAKE YOUR LIFE EASIER, DUMBASS

----------

Parse a json file to server to signal manager accountIDs, let server apply
automatically.

--> create a json file locally with manager accountIDs,
    (comment out username (#Dasshu), 1975253)
    >>> **IMPORTANT: SAVE TO ONEDRIVE OR GOOGLE SERVICES** <<<

--> upload file to server, server reads accountIDs separated by semicolons (;) and
    applies manager to said accountIDs. example:
                                                    #Dasshu
                                                    1975253;

--> use Argon (https://github.com/GlobedGD/argon) to capture user tokens and allow
    for Gauntlet management on button press using Argon auth tokens, thus removing
    dependency for passwords entirely!

--> connect in-game Gauntlet management panel to
https://bettergauntlets.dev/login, gauntlets, ismanager, etc.

----------